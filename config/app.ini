app_mode = production
version = 0.1.0-alpha

[paths]
project_root = . # if you keep the project root somewhere else than /home/git/sorcia, then provide the absolute path
repo_path = ./repositories # if you have repository somewhere else, then provide the absolute path. It's better, if you keep it at /home/git/repositories
db_path = .

[server]
http_port = 1937
