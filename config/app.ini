app_mode = self-hosted
version = 0.1.0

[paths]
project_root = . # if you keep the project root somewhere else than /home/git/sorcia, then provide the absolute path
repo_path = . # if you have repository somewhere else, then provide the absolute path. It's better, if you keep it at /home/git/repositories

[server]
http_port = 1937

[postgres]
hostname = localhost # localhost or external IP of your postgresql database server
port = 5432
name = sorciadb
username = sorcia
password = magicpotion
sslmode = disable # either "disable", "require" or "verify-full"
